apiVersion: v1
kind: Service
metadata:
  name: {{ include "camunda-backup-controller.fullname" . }}
  labels:
    {{- include "camunda-backup-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camunda-backup-controller.selectorLabels" . | nindent 4 }}
